FROM python:3.12-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    nodejs npm \
    && rm -rf /var/lib/apt/lists/*

RUN useradd -u 1001 -m sandbox
WORKDIR /workspace
USER sandbox

COPY runner.py /runner.py

ENTRYPOINT ["python", "/runner.py"]
